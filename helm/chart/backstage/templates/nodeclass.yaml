apiVersion: karpenter.k8s.aws/v1
kind: EC2NodeClass
metadata:
  name: {{ include "backstage.fullname" . }}-nodeclass
  labels:
    {{- include "common.labels" . | nindent 4 }}
spec:
  role: {{ .Values.deploymentId }}-core-node
  amiSelectorTerms:
    - alias: bottlerocket@latest
  subnetSelectorTerms:
    - tags:
        karpenter.sh/discovery: {{ .Values.deploymentId }}-vpc
        "{{ .Values.domain }}:type": private_with_egress
  securityGroupSelectorTerms:
    - tags:
        "aws:eks:cluster-name": "{{ .Values.deploymentId }}-eks"
  blockDeviceMappings:
    - deviceName: /dev/xvda
      ebs:
        volumeSize: 50Gi
        volumeType: gp3
        encrypted: true
        deleteOnTermination: true
    - deviceName: /dev/xvdb
      ebs:
        volumeSize: 100Gi
        volumeType: gp3
        encrypted: true
        deleteOnTermination: true
  tags:
    Name: {{ include "backstage.fullname" . }}-node
    {{- range $key, $value := .Values.labels }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
